#summary(att.lmer);
# Get p-values with CAR package
Anova(att.lmer,type=3)
Anova(sat.lmer,type=3)
Anova(conf.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/Analyses/R mixed-effects analysis")
# Read in the data
data <- read.table(header=T,"LMM_separateMeasures_4regions.xls")
# Fit the mixed-effects models with tms_site being a fixed effect and subject a random effect
att.lmer = lmer(Attention ~ tms_site + attFMRI + (1 | Subject + attFMRI), data=data);#, weights = attFMRI);
sat.lmer = lmer(SAT ~ tms_site + (1 | Subject), data=data, weights = satFMRI);
conf.lmer = lmer(Conf ~ tms_site + (1 | Subject), data=data, weights = confFMRI);
# Summarise model output
#summary(att.lmer);
# Get p-values with CAR package
Anova(att.lmer,type=3)
Anova(sat.lmer,type=3)
Anova(conf.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/Analyses/R mixed-effects analysis")
# Read in the data
data <- read.table(header=T,"LMM_separateMeasures_4regions.xls")
# Fit the mixed-effects models with tms_site being a fixed effect and subject a random effect
att.lmer = lmer(Attention ~ tms_site + attFMRI + (1 | attFMRI), data=data);#, weights = attFMRI);
sat.lmer = lmer(SAT ~ tms_site + (1 | Subject), data=data, weights = satFMRI);
conf.lmer = lmer(Conf ~ tms_site + (1 | Subject), data=data, weights = confFMRI);
# Summarise model output
#summary(att.lmer);
# Get p-values with CAR package
Anova(att.lmer,type=3)
Anova(sat.lmer,type=3)
Anova(conf.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/Analyses/R mixed-effects analysis")
# Read in the data
data <- read.table(header=T,"LMM_separateMeasures_4regions.xls")
# Fit the mixed-effects models with tms_site being a fixed effect and subject a random effect
att.lmer = lmer(Attention ~ tms_site + (1 | attFMRI), data=data);#, weights = attFMRI);
sat.lmer = lmer(SAT ~ tms_site + (1 | Subject), data=data, weights = satFMRI);
conf.lmer = lmer(Conf ~ tms_site + (1 | Subject), data=data, weights = confFMRI);
# Summarise model output
#summary(att.lmer);
# Get p-values with CAR package
Anova(att.lmer,type=3)
Anova(sat.lmer,type=3)
Anova(conf.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/Analyses/R mixed-effects analysis")
# Read in the data
data <- read.table(header=T,"LMM_separateMeasures_4regions.xls")
# Fit the mixed-effects models with tms_site being a fixed effect and subject a random effect
att.lmer = lmer(Attention ~ tms_site + attFMRI + (1|Subject) + (1|attFMRI), data=data);#, weights = attFMRI);
sat.lmer = lmer(SAT ~ tms_site + (1 | Subject), data=data, weights = satFMRI);
conf.lmer = lmer(Conf ~ tms_site + (1 | Subject), data=data, weights = confFMRI);
# Summarise model output
#summary(att.lmer);
# Get p-values with CAR package
Anova(att.lmer,type=3)
Anova(sat.lmer,type=3)
Anova(conf.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/Analyses/R mixed-effects analysis")
# Read in the data
data <- read.table(header=T,"LMM_separateMeasures_4regions.xls")
# Fit the mixed-effects models with tms_site being a fixed effect and subject a random effect
att.lmer = lmer(Attention ~ tms_site + (1|Subject) + (1|attFMRI), data=data);#, weights = attFMRI);
sat.lmer = lmer(SAT ~ tms_site + (1 | Subject), data=data, weights = satFMRI);
conf.lmer = lmer(Conf ~ tms_site + (1 | Subject), data=data, weights = confFMRI);
# Summarise model output
#summary(att.lmer);
# Get p-values with CAR package
Anova(att.lmer,type=3)
Anova(sat.lmer,type=3)
Anova(conf.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/Analyses/R mixed-effects analysis")
# Read in the data
data <- read.table(header=T,"LMM_separateMeasures_4regions.xls")
# Fit the mixed-effects models with tms_site being a fixed effect and subject a random effect
att.lmer = lmer(Attention ~ tms_site + (1|Subject) + (1 + tms_site|attFMRI), data=data);#, weights = attFMRI);
sat.lmer = lmer(SAT ~ tms_site + (1 | Subject), data=data, weights = satFMRI);
conf.lmer = lmer(Conf ~ tms_site + (1 | Subject), data=data, weights = confFMRI);
# Summarise model output
#summary(att.lmer);
# Get p-values with CAR package
Anova(att.lmer,type=3)
Anova(sat.lmer,type=3)
Anova(conf.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/Analyses/R mixed-effects analysis")
# Read in the data
data <- read.table(header=T,"LMM_separateMeasures_4regions.xls")
# Fit the mixed-effects models with tms_site being a fixed effect and subject a random effect
att.lmer = lmer(Attention ~ tms_site + (1 + tms_site|attFMRI), data=data);#, weights = attFMRI);
sat.lmer = lmer(SAT ~ tms_site + (1 | Subject), data=data, weights = satFMRI);
conf.lmer = lmer(Conf ~ tms_site + (1 | Subject), data=data, weights = confFMRI);
# Summarise model output
#summary(att.lmer);
# Get p-values with CAR package
Anova(att.lmer,type=3)
Anova(sat.lmer,type=3)
Anova(conf.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/Analyses/R mixed-effects analysis")
# Read in the data
data <- read.table(header=T,"LMM_separateMeasures_4regions.xls")
# Fit the mixed-effects models with tms_site being a fixed effect and subject a random effect
att.lmer = lmer(Attention ~ tms_site + (tms_site|attFMRI), data=data);#, weights = attFMRI);
sat.lmer = lmer(SAT ~ tms_site + (1 | Subject), data=data, weights = satFMRI);
conf.lmer = lmer(Conf ~ tms_site + (1 | Subject), data=data, weights = confFMRI);
# Summarise model output
#summary(att.lmer);
# Get p-values with CAR package
Anova(att.lmer,type=3)
Anova(sat.lmer,type=3)
Anova(conf.lmer,type=3)
att.lmer = lmer(Attention ~ tms_site + (tms_site|attFMRI), data=data);
att.lmer = lmer(Attention ~ tms_site + (1+tms_site|attFMRI), data=data);
att.lmer = lmer(Attention ~ tms_site + (1+tms_site|Subject), data=data);
att.lmer = lmer(Attention ~ tms_site + (1|Subject), data=data);
att.lmer = lmer(Attention ~ tms_site + (1|Subject) + (1|attFMRI), data=data);
att.lmer = lmer(Attention ~ tms_site/attFMRI + (1|Subject), data=data);
Anova(att.lmer,type=3)
display(att.lmer)
summarylay(att.lmer)
summary(att.lmer)
sat.lmer = lmer(SAT ~ tms_site/attFMRI + (1|Subject), data=data);
Anova(sat.lmer,type=3)
summary(sat.lmer)
sat.lmer = lmer(SAT ~ tms_site/satFMRI + (1|Subject), data=data);
Anova(sat.lmer,type=3)
summary(sat.lmer)
conf.lmer = lmer(Conf ~ tms_site + (1 | Subject), data=data, weights = confFMRI);
conf.lmer = lmer(Conf ~ tms_site/confFMRI + (1 | Subject), data=data);
Anova(conf.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/Analyses/R mixed-effects analysis")
# Read in the data
data <- read.table(header=T,"LMM_separateMeasures_4regions.xls")
# Fit the mixed-effects models with tms_site being a fixed effect and subject a random effect
att.lmer = lmer(Attention ~ tms_site + (tms_site|attFMRI), data=data);#, weights = attFMRI);
sat.lmer = lmer(SAT ~ 1 + tms_site + (1 | Subject), data=data, weights = satFMRI);
conf.lmer = lmer(Conf ~ tms_site + (1 | Subject), data=data, weights = confFMRI);
# Summarise model output
#summary(att.lmer);
# Get p-values with CAR package
Anova(att.lmer,type=3)
Anova(sat.lmer,type=3)
Anova(conf.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/Analyses/R mixed-effects analysis")
# Read in the data
data <- read.table(header=T,"LMM_separateMeasures_4regions.xls")
# Fit the mixed-effects models with tms_site being a fixed effect and subject a random effect
att.lmer = lmer(Attention ~ tms_site + (tms_site|attFMRI), data=data);#, weights = attFMRI);
sat.lmer = lmer(SAT ~ tms_site + (1 | Subject), data=data, weights = satFMRI);
conf.lmer = lmer(Conf ~ tms_site + (1 | Subject), data=data, weights = confFMRI);
# Summarise model output
#summary(att.lmer);
# Get p-values with CAR package
Anova(att.lmer,type=3)
Anova(sat.lmer,type=3)
Anova(conf.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/Analyses/R mixed-effects analysis")
# Read in the data
data <- read.table(header=T,"LMM_separateMeasures_4regions.xls")
# Fit the mixed-effects models with tms_site being a fixed effect and subject a random effect
att.lmer = lmer(Attention ~ 1 + tms_site + (1 + tms_site|Subject), data=data);#, weights = attFMRI);
sat.lmer = lmer(SAT ~ 1 + tms_site + (1 + tms_site| Subject), data=data, weights = satFMRI);
conf.lmer = lmer(Conf ~ 1 + tms_site + (1 + tms_site| Subject), data=data, weights = confFMRI);
# Summarise model output
#summary(att.lmer);
# Get p-values with CAR package
Anova(att.lmer,type=3)
Anova(sat.lmer,type=3)
Anova(conf.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/Analyses/R mixed-effects analysis")
# Read in the data
data <- read.table(header=T,"LMM_separateMeasures_4regions.xls")
# Fit the mixed-effects models with tms_site being a fixed effect and subject a random effect
att.lmer = lmer(Attention ~ 1 + tms_site + (1|Subject), data=data);#, weights = attFMRI);
sat.lmer = lmer(SAT ~ 1 + tms_site + (1|Subject), data=data, weights = satFMRI);
conf.lmer = lmer(Conf ~ 1 + tms_site + (1|Subject), data=data, weights = confFMRI);
# Summarise model output
#summary(att.lmer);
# Get p-values with CAR package
Anova(att.lmer,type=3)
Anova(sat.lmer,type=3)
Anova(conf.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/Analyses/R mixed-effects analysis")
# Read in the data
data <- read.table(header=T,"LMM_separateMeasures_4regions.xls")
# Fit the mixed-effects models with tms_site being a fixed effect and subject a random effect
att.lmer = lmer(Attention ~ 1 + tms_site + (1|Subject), data=data, weights = attFMRI);
sat.lmer = lmer(SAT ~ 1 + tms_site + (1|Subject), data=data, weights = satFMRI);
conf.lmer = lmer(Conf ~ 1 + tms_site + (1|Subject), data=data, weights = confFMRI);
# Summarise model output
#summary(att.lmer);
# Get p-values with CAR package
Anova(att.lmer,type=3)
Anova(sat.lmer,type=3)
Anova(conf.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/Analyses/R mixed-effects analysis")
# Read in the data
data <- read.table(header=T,"LMM_separateMeasures_4regions.xls")
# Fit the mixed-effects models with tms_site being a fixed effect and subject a random effect
att.lmer = lmer(Attention ~ 1 + tms_site + attFMRI + (1|Subject), data=data);#, weights = attFMRI);
sat.lmer = lmer(SAT ~ 1 + tms_site + (1|Subject), data=data, weights = satFMRI);
conf.lmer = lmer(Conf ~ 1 + tms_site + (1|Subject), data=data, weights = confFMRI);
# Summarise model output
#summary(att.lmer);
# Get p-values with CAR package
Anova(att.lmer,type=3)
Anova(sat.lmer,type=3)
Anova(conf.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/Analyses/R mixed-effects analysis")
# Read in the data
data <- read.table(header=T,"LMM_separateMeasures_4regions.xls")
# Fit the mixed-effects models with tms_site being a fixed effect and subject a random effect
att.lmer = lmer(Attention ~ 1 + tms_site + (1|Subject), data=data);#, weights = attFMRI);
sat.lmer = lmer(SAT ~ 1 + tms_site + (1|Subject), data=data, weights = satFMRI);
conf.lmer = lmer(Conf ~ 1 + tms_site + (1|Subject), data=data, weights = confFMRI);
# Summarise model output
#summary(att.lmer);
# Get p-values with CAR package
Anova(att.lmer,type=3)
Anova(sat.lmer,type=3)
Anova(conf.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/Analyses/R mixed-effects analysis")
# Read in the data
data <- read.table(header=T,"LMM_separateMeasures_4regions.xls")
# Fit the mixed-effects models with tms_site being a fixed effect and subject a random effect
att.lmer = lmer(Attention ~ 1 + tms_site + (1|Subject), data=data, weights = attFMRI);
sat.lmer = lmer(SAT ~ 1 + tms_site + (1|Subject), data=data, weights = satFMRI);
conf.lmer = lmer(Conf ~ 1 + tms_site + (1|Subject), data=data, weights = confFMRI);
# Summarise model output
#summary(att.lmer);
# Get p-values with CAR package
Anova(att.lmer,type=3)
Anova(sat.lmer,type=3)
Anova(conf.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/Analyses/R mixed-effects analysis")
# Read in the data
data <- read.table(header=T,"LMM_separateMeasures_4regions.xls")
# Fit the mixed-effects models with tms_site being a fixed effect and subject a random effect
att.lmer = lmer(Attention ~ 1 + tms_site + (1|Subject), data=data, weights = attFMRI);
sat.lmer = lmer(SAT ~ 1 + tms_site + (1|Subject), data=data, weights = satFMRI);
conf.lmer = lmer(Conf ~ 1 + tms_site + (1|Subject), data=data, weights = confFMRI);
# Summarise model output
#summary(att.lmer);
# Get p-values with CAR package
Anova(att.lmer,type=3)
Anova(sat.lmer,type=3)
Anova(conf.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/TBS analyses/Analyses/")
# Read in the data
data <- read.table(header=T,"LMM_allMeasures.xls")
# Fit the Linear Mixed-Effects Model
interaction_weight.lmer = lmer(y ~ tms_site*measure + (1 + tms_site + measure | Subject), data=data, weights = weights);
interaction.lmer = lmer(y ~ tms_site*measure + (1 + tms_site + measure | Subject), data=data, REML=FALSE);
#no_interaction.lmer = lmer(y ~ tms_site + measure + (1 + tms_site + measure | Subject), data=data, REML=FALSE);
# Summarise model output
#summary(interaction.lmer);
# Compare the two models
anova(interaction_weight.lmer,interaction.lmer)
# Get p-values with CAR package
Anova(interaction_weight.lmer,type=3)
Anova(interaction.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/TBS analyses/Analyses/")
# Read in the data
data <- read.table(header=T,"LMM_allMeasures.xls")
# Fit the Linear Mixed-Effects Model
interaction.lmer = lmer(y ~ tms_site*measure + (1 + tms_site + measure | Subject), data=data, REML=FALSE);
no_interaction.lmer = lmer(y ~ tms_site + measure + (1 + tms_site + measure | Subject), data=data, REML=FALSE);
# Summarise model output
#summary(interaction.lmer);
# Get p-values with CAR package
Anova(interaction.lmer,type=3)
Anova(no_interaction.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/TBS analyses/Analyses/")
# Read in the data
data <- read.table(header=T,"LMM_allMeasures.xls")
# Fit the Linear Mixed-Effects Model
interaction.lmer = lmer(y ~ tms_site*measure + (1 + tms_site + measure | Subject), data=data, REML=FALSE);
# Summarise model output
#summary(interaction.lmer);
# Get p-values with CAR package
Anova(interaction.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/TBS analyses/Analyses/")
# Read in the data
data <- read.table(header=T,"LMM_allMeasures.xls")
# Fit the Linear Mixed-Effects Model
interaction.lmer = lmer(y ~ tms_site*measure + (1 + tms_site * measure | Subject), data=data, REML=FALSE);
# Summarise model output
#summary(interaction.lmer);
# Get p-values with CAR package
Anova(interaction.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/Analyses/R mixed-effects analysis")
# Read in the data
data <- read.table(header=T,"LMM_separateMeasures_4regions.xls")
# Fit the mixed-effects models with tms_site being a fixed effect and subject a random effect
att.lmer = lmer(Attention ~ 1 + tms_site + (1|Subject), data=data, weights = attFMRI);
sat.lmer = lmer(SAT ~ 1 + tms_site + (1|Subject), data=data, weights = satFMRI);
conf.lmer = lmer(Conf ~ 1 + tms_site + (1|Subject), data=data, weights = confFMRI);
# Summarise model output
#summary(att.lmer);
# Get p-values with CAR package
Anova(att.lmer,type=3)
Anova(sat.lmer,type=3)
Anova(conf.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/TBS analyses/Analyses/")
# Read in the data
data <- read.table(header=T,"LMM_allMeasures.xls")
# Fit the Linear Mixed-Effects Model
interaction.lmer = lmer(y ~ tms_site*measure + (1 + tms_site + measure | Subject), data=data, REML=FALSE);
# Summarise model output
#summary(interaction.lmer);
# Get p-values with CAR package
Anova(interaction.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
setwd("~/Dropbox/current_projects/TBS_to_PFC/TBS analyses/Analyses/")
# Read in the data
data <- read.table(header=T,"LMM_allMeasures.xls")
# Fit the Linear Mixed-Effects Model
interaction.lmer = lmer(y ~ tms_site*measure + (1 + tms_site + measure | Subject), data=data, REML=FALSE);
# Summarise model output
#summary(interaction.lmer);
# Get p-values with CAR package
Anova(interaction.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Set current directory
#setwd("~/Dropbox/current_projects/TBS_to_PFC/TBS analyses/Analyses/")
# Read in the data
data <- read.table(header=T,"LMM_allMeasures.xls")
# Fit the Linear Mixed-Effects Model
interaction.lmer = lmer(y ~ tms_site*measure + (1 + tms_site + measure | Subject), data=data, REML=FALSE);
# Summarise model output
summary(interaction.lmer);
# Get p-values with CAR package
Anova(interaction.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Read in the data
data <- read.table(header=T,"LMM_separateMeasures_4regions.xls")
# Fit the mixed-effects models with tms_site being a fixed effect and subject a random effect
att.lmer = lmer(Attention ~ 1 + tms_site + (1|Subject), data=data, weights = attFMRI);
sat.lmer = lmer(SAT ~ 1 + tms_site + (1|Subject), data=data, weights = satFMRI);
type2AUC.lmer = lmer(Conf ~ 1 + tms_site + (1|Subject), data=data, weights = confFMRI);
# Get p-values with CAR package
Anova(att.lmer,type=3)
Anova(sat.lmer,type=3)
Anova(type2AUC.lmer,type=3)
library(Matrix)
library(lme4)
library(car)
# Clear the workspace
rm(list=ls())
# Read in the data
data <- read.table(header=T,"LMM_separateMeasures_4regions.xls")
# Fit the mixed-effects models with tms_site being a fixed effect and subject a random effect
att.lmer = lmer(Attention ~ 1 + tms_site + (1|Subject), data=data, weights = attFMRI);
sat.lmer = lmer(SAT ~ 1 + tms_site + (1|Subject), data=data, weights = satFMRI);
type2AUC.lmer = lmer(Conf ~ 1 + tms_site + (1|Subject), data=data, weights = confFMRI);
# Summarise models
summary(att.lmer);
summary(sat.lmer);
summary(type2AUC.lmer);
# Get p-values with CAR package
Anova(att.lmer,type=3)
Anova(sat.lmer,type=3)
Anova(type2AUC.lmer,type=3)
